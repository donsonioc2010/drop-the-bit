<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="parking.member.dao.MemberDao">

    <insert id="insert" parameterType="member">
        INSERT INTO member(user_id, password, member_type)
        VALUES (#{userId}, sha1(#{password}), #{memberType})
    </insert>

    <update id="update" parameterType="Member">
        UPDATE member
        SET password=sha1(#{password}), member_type=#{memberType}, updated_at=NOW()
        WHERE id=#{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM member WHERE id=#{id}
    </delete>

    <delete id="deleteByMember" parameterType="Member">
        DELETE FROM member WHERE id=#{id}
    </delete>

    <select id="findAll" resultType="Member">
        SELECT
        id, user_id, member_type, created_at, updated_at
        FROM member
        ORDER BY id ASC
    </select>

    <select id="findByUserIdCount" resultType="int" parameterType="String">
        SELECT count(1) FROM member WHERE user_id=#{userId}
    </select>

    <select id="findById" resultType="Member" parameterType="Long">
        SELECT id, user_id, member_type, created_at, updated_at
        FROM member
        WHERE id=#{id}
    </select>

    <select id="findByUserIdAndPassword" parameterType="Member" resultType="Member">
        SELECT
        id, user_id, member_type, created_at, updated_at
        FROM member
        WHERE user_id=#{userId} and password=sha1(#{password})
    </select>
</mapper>